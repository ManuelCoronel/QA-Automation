import {test} from '@playwright/test';
import { SearchForAcomodationPage } from '../src/pages/SearchForAcomodationPage';

/*
test.beforeEach(async({page})=>{
await page.goto('https://www.booking.com');
})

test(`
Given Necesito buscar un alojamiento
when lleno los campos
then visualizo los alojamientos disponibles`,async({page}) => {
    const searchForAcomodationPage = new SearchForAcomodationPage(page);
    
})
*/